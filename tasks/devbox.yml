---
version: '3'

vars:
  DEVBOX_VAR: "devbox"
  DEVBOX_HOST: "{{.DEVBOX_VAR}}.{{.GLOBAL_DOMAIN}}"
  DEVBOX_TF_DIR: "{{.TERRAFORM_DIR}}/{{.DEVBOX_VAR}}"
  DEVBOX_TF_STATE: "{{.TERRAFORM_STATE}}/{{.DEVBOX_VAR}}.tfstate"
  DEVBOX_ANSIBLE_PLAYBOOK: "lxc-{{.DEVBOX_VAR}}-provision.yml"

tasks:
  check:
    desc: "Check '{{.DEVBOX_HOST}}' host vars"
    cmds:
      - "echo DEVBOX_HOST={{.DEVBOX_HOST}}"
      - "echo DEVBOX_TF_DIR={{.DEVBOX_TF_DIR}}"
      - "echo DEVBOX_TF_STATE={{.DEVBOX_TF_STATE}}"
      - "echo DEVBOX_ANSIBLE_PLAYBOOK={{.DEVBOX_ANSIBLE_PLAYBOOK}}"

  # ? Terraform
  create-lxc:
    desc: "Create '{{.DEVBOX_HOST}}' host"
    dir: "{{.DEVBOX_TF_DIR}}"
    cmds:
      - terraform init -backend-config="path={{.DEVBOX_TF_STATE}}"
      - terraform fmt && terraform validate
      - terraform apply -lock=false
      - terraform output

  destroy-lxc:
    desc: "Destroy '{{.DEVBOX_HOST}}' host"
    dir: "{{.DEVBOX_TF_DIR}}"
    cmds:
      - terraform destroy -lock=false -compact-warnings

  update-dns:
    desc: "Update DNS records for '{{.DEVBOX_HOST}}' host"
    dir: "{{.DEVBOX_TF_DIR}}"
    cmds:
      - terraform init -backend-config="path={{.DEVBOX_TF_STATE}}"
      - terraform fmt && terraform validate
      - terraform apply -lock=false -compact-warnings -target=module.my_lxc.cloudflare_record.cname_records

  # ? Ansible
  provision:
    desc: "Provision '{{.DEVBOX_HOST}}' via Ansible"
    dir: "{{.ANSIBLE_DIR}}"
    cmds:
      - "ansible-playbook -i {{.ANSIBLE_INVENTORY_PROXMOX}} {{.DEVBOX_ANSIBLE_PLAYBOOK}}"
