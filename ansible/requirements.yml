---
collections:
  - name: ansible.netcommon
    version: ">=7.2.0"

  - name: ansible.posix
    version: ">=1.6.2"

  - name: ansible.utils
    version: ">=5.1.2"

  - name: community.general
    version: ">=10.4.0"

  - name: community.docker
    version: ">=4.6.1"

  - name: community.proxmox
    version: ">=1.0.1"
